//Operadores OU || E &&, '=' recebe, '==' igual com conversão, '===' igual sem conversão
const idade = 8;
const nome = 'Cleber'

if(idade >= 18) {
    console.log('Acesso permitido!')
} else {
    console.log('Acesso negado!')
}